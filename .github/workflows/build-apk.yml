name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Publish APK release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v4-fingerprint
          name: حضورینو ۴ — نسخه اثر انگشت گوشی شخصی
          body: نسخه جدید کاملاً آفلاین؛ بدون تشخیص چهره و دوربین، دارای فعال‌سازی با کد پرسنلی و ثبت ورود و خروج با اثر انگشت یا رمز شخصی.
          prerelease: true
          overwrite_files: true
          files: app/build/outputs/apk/debug/app-debug.apk
